#include<iostream>
using namespace std;

int main() {
    int row col;
    cout << "Enter row and column: ";
    cin >> row >> col:
    for (int i = 1; i <= row; ++i) {
        for (int j = 1; j <= col; j++) {
           if (row < 2 && col < ) 
        }
    }
    return 0;
}
